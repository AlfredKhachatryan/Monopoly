// The login page moved to src/Login/ when it was redesigned (see
// src/Login/LoginScreen.jsx). This file stays as the route's address so
// src/main.jsx — and anything else that imports "./Pages/Login" — keeps
// working untouched.

export { Login } from "../Login/LoginScreen";
