# Loaded by `npm run dev:mock` (vite --mode mock). Not a secret -- just the
# flag vite.config.js checks to decide whether to swap in
# src/dev/mockSupabase.js for src/Hooks/supabase.jsx. See TESTING.md,
# "Offline client preview".
VITE_MOCK=1
